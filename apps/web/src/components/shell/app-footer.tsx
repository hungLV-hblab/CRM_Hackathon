/**
 * One line, and it stays one line. A footer in an internal CRM earns its space only by
 * answering "which build am I looking at" during a demo — anything more is chrome.
 */
export function AppFooter() {
  return (
    <footer className="border-t border-ink-200 px-4 py-3 text-xs text-ink-500">
      CRM nội bộ HBLAB · bản dựng nội bộ
    </footer>
  )
}
